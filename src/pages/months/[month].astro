---
import FinanceApp from '../../components/FinanceApp';
import { isSelectableMonthKey, loadDashboardData } from '../../server/dashboard';
import '../../styles/global.css';

const month = Astro.params.month;

if (!isSelectableMonthKey(month)) {
  return Astro.redirect('/');
}

const dashboardData = await loadDashboardData({ month });
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="description" content="A read-only Firefly III finance review UI." />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <title>{dashboardData.period.label} | Finances</title>
  </head>
  <body>
    <FinanceApp client:load initialData={dashboardData} />
  </body>
</html>
